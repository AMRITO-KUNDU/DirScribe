# DirScribe

DirScribe is a Python tool that generates comprehensive text summaries of software projects, including directory structure and file contents. It’s perfect for code reviews, documentation, and sharing full codebases with AI tools or collaborators.

---

## ✨ Features

- **Directory Tree Generation** — Creates a visual tree structure of your project  
- **File Content Extraction** — Extracts contents from text-based files  
- **Smart Filtering** — Ignores common directories (`node_modules`, `.git`, etc.)  
- **Multiple Input Methods** — CLI, GUI folder picker, or interactive mode  
- **Configurable Extensions** — Supports 50+ file types  
- **Cross-Platform** — Works on Windows, macOS, and Linux  

---

## ⚙️ Installation

### Prerequisites

- Python 3.6 or higher  
- Tkinter *(optional, for GUI mode — usually pre-installed)*  

### Setup

```bash
# Clone the repository
git clone https://github.com/yourusername/dirscribe.git
cd dirscribe

# Optional: install enhanced terminal UI
pip install rich
